import { RemovableDirective } from './removable.directive';

describe('RemovableDirective', () => {
  it('should create an instance', () => {
    const directive = new RemovableDirective();
    expect(directive).toBeTruthy();
  });
});
